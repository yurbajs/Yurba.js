{
  "buildCommand": "pnpm run guide:build",
  "outputDirectory": ".vitepress/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/guide"
}
